#include <stdio.h>
#include <math.h>

int main()
{
  float n,e,z=1;
  float sum=0;
  float i;
  float formula;
  float sum2=0,sum3=0;
  float m,j=1;
  
  
  printf("enter i\n");
  scanf("%f",&i);
  
  for (n=1;n<=i;n++)
  {
      e=n*(n+1);
      sum=sum+e;
      //printf("n=%d sum=%d\n" ,n,sum);
  
  formula = (n*(n+1)*(n+2))/3;
  }
  printf("loop result = %1.0f\n" ,sum);
  printf("formula = %1.0f\n",formula); 
  

  while (j<=i)
  {
        
        
        m=(1/(j*(j+1)));
        sum2 = sum2 + m;
        j=j+1;
        
  }
        
  formula = 1-(1/(i+1));
  
  printf("loop result = %f\n" ,sum2);
  printf("formula = %f\n",formula);
  
  do 
  {
   
   sum3 = sum3 + (1/(pow(2,z)));
   z = z + 1;
   formula= 1-(1/(pow(2,i)));
  }
  while (z<=i);
  printf("loop result = %f\n" ,sum3);
  printf("formula = %f\n" ,formula);
  
  system("pause");
  return 0;
}
